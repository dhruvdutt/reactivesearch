# ReactiveSearch Website

## Installation

- `yarn`

## Usage

- `yarn start` starts live-reloading dev server with Babel, Sass, and Autoprefixer
- `yarn run imagemin` minifies images from `src/img` to `dist/img`. Put raw images in `src/img` and run this.
